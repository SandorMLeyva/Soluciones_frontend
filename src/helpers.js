export function getRndInteger(min, max) {
    return Math.floor(Math.random() * (max - min)) + min;
}

export const COOLORS = {
    'UPEN': '#EA4335', 
    'APEN': '#4285F4',
    'PROC': '#FBBC05',
    'FIN': '#34A853',
    'WARR': '#66757F',
    'NO_WARR': '#FFFFFF'
};
